<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:SrvSurvey.Desktop"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="using:SrvSurvey.Desktop.Controls"
        xmlns:vm="using:SrvSurvey.Desktop.ViewModels"
        x:Class="SrvSurvey.Desktop.SurfaceSurveyOverlayWindow"
        x:DataType="vm:SurfaceSurveyOverlayViewModel"
        mc:Ignorable="d"
        d:DesignWidth="380"
        d:DesignHeight="500"
        Width="{Binding WindowWidth}"
        Height="{Binding WindowHeight}"
        MinWidth="250"
        MinHeight="240"
        MaxWidth="440"
        MaxHeight="600"
        CanResize="False"
        ShowActivated="False"
        ShowInTaskbar="False"
        WindowDecorations="None"
        Topmost="True"
        Background="Transparent"
        TransparencyBackgroundFallback="Transparent"
        TransparencyLevelHint="Transparent"
        Title="SrvSurvey surface survey overlay">
  <Border Background="Transparent"
          BorderThickness="0">
    <local:SurfaceSurveyOverlayPresentation />
  </Border>
</Window>
