<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:SrvSurvey.Desktop.Controls"
             xmlns:vm="using:SrvSurvey.Desktop.ViewModels"
             x:Class="SrvSurvey.Desktop.GuardianStatusOverlayPresentation"
             x:DataType="vm:GuardianOverlayViewModel"
             MaxWidth="260"
             HorizontalAlignment="Left"
             ClipToBounds="True">
  <UserControl.Styles>
    <Style Selector="Border.guardian-legacy-choice">
      <Setter Property="MinWidth" Value="72" />
      <Setter Property="MinHeight" Value="40" />
      <Setter Property="Padding" Value="8,6" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="BorderBrush" Value="{DynamicResource RavenGuardianPrimaryDimBrush}" />
      <Setter Property="Background" Value="{DynamicResource RavenGuardianSurfaceBrush}" />
      <Setter Property="CornerRadius" Value="6" />
    </Style>
    <Style Selector="Border.guardian-legacy-choice.selected">
      <Setter Property="BorderThickness" Value="2" />
      <Setter Property="BorderBrush" Value="{DynamicResource RavenGuardianPrimaryBrush}" />
      <Setter Property="Background" Value="{DynamicResource RavenGuardianSecondaryBrush}" />
    </Style>
    <Style Selector="Border.guardian-legacy-choice.selected TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource RavenGuardianBackgroundBrush}" />
      <Setter Property="FontWeight" Value="Bold" />
    </Style>
    <Style Selector="TextBlock.guardian-artifact.missing">
      <Setter Property="Foreground" Value="{DynamicResource RavenGuardianDangerBrush}" />
    </Style>
  </UserControl.Styles>
  <Border Classes="guardian-panel" MinHeight="108">
    <Grid>
    <Grid IsVisible="{Binding Guardian.IsGlideApproach}"
          RowDefinitions="Auto,Auto,*,Auto">
      <TextBlock Classes="overlay-header type-header"
                 Text="{Binding Guardian.GlideApproachTitle}"
                 TextWrapping="Wrap" />
      <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
      <TextBlock Grid.Row="2"
                 Classes="guardian-legacy-middle guardian-legacy-cyan type-body"
                 VerticalAlignment="Center"
                 Text="{Binding Guardian.GlideApproachText}"
                 TextWrapping="Wrap" />
      <TextBlock Grid.Row="3"
                 Classes="guardian-legacy-small type-detail"
                 Text="{Binding Guardian.GlideApproachFooter}"
                 TextWrapping="Wrap" />
    </Grid>

    <Grid IsVisible="{Binding Guardian.IsLocalGuardianStatus}">
      <Grid RowDefinitions="Auto,Auto,*,Auto"
            IsVisible="{Binding Guardian.IsGuardianSiteTypeChoiceVisible}">
        <TextBlock Classes="overlay-header type-header"
                   Text="{Binding Guardian.GuardianStatusTitle}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
        <Grid Grid.Row="2"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              ColumnDefinitions="Auto,Auto,Auto"
              ColumnSpacing="10">
          <Border Classes="guardian-legacy-choice"
                  Classes.selected="{Binding Guardian.IsGuardianChoiceOneSelected}">
            <Grid>
              <TextBlock Classes="guardian-legacy-small type-detail" Text="A:" />
              <TextBlock Classes="guardian-legacy-middle type-body"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Text="{Binding Guardian.GuardianChoiceOneText}"
                         TextAlignment="Center"
                         TextWrapping="Wrap" />
            </Grid>
          </Border>
          <Border Grid.Column="1"
                  Classes="guardian-legacy-choice"
                  Classes.selected="{Binding Guardian.IsGuardianChoiceTwoSelected}">
            <Grid>
              <TextBlock Classes="guardian-legacy-small type-detail" Text="B:" />
              <TextBlock Classes="guardian-legacy-middle type-body"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Text="{Binding Guardian.GuardianChoiceTwoText}"
                         TextAlignment="Center"
                         TextWrapping="Wrap" />
            </Grid>
          </Border>
          <Border Grid.Column="2"
                  Classes="guardian-legacy-choice"
                  Classes.selected="{Binding Guardian.IsGuardianChoiceThreeSelected}">
            <Grid>
              <TextBlock Classes="guardian-legacy-small type-detail" Text="C:" />
              <TextBlock Classes="guardian-legacy-middle type-body"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Text="{Binding Guardian.GuardianChoiceThreeText}"
                         TextAlignment="Center"
                         TextWrapping="Wrap" />
            </Grid>
          </Border>
        </Grid>
        <TextBlock Grid.Row="3"
                   Classes="guardian-legacy-small guardian-legacy-cyan type-detail"
                   Text="{Binding Guardian.GuardianChoiceGestureText}"
                   TextWrapping="Wrap" />
      </Grid>

      <Grid RowDefinitions="Auto,Auto,*,Auto"
            IsVisible="{Binding Guardian.IsGuardianHeadingChoiceVisible}">
        <TextBlock Classes="overlay-header type-header"
                   Text="{Binding Guardian.GuardianStatusTitle}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
        <TextBlock Grid.Row="2"
                   Classes="guardian-legacy-middle type-body"
                   VerticalAlignment="Center"
                   Text="{Binding Guardian.GuardianStatusDetail}"
                   TextWrapping="Wrap" />
        <TextBlock Grid.Row="3"
                   Classes="guardian-legacy-small guardian-legacy-cyan type-detail"
                   Text="{Binding Guardian.BlinkGestureText}"
                   TextWrapping="Wrap" />
      </Grid>

      <Grid RowDefinitions="Auto,Auto,*,Auto"
            IsVisible="{Binding Guardian.IsGuardianOriginVisible}">
        <TextBlock Classes="overlay-header type-header"
                   Text="{Binding Guardian.GuardianStatusTitle}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
        <TextBlock Grid.Row="2"
                   Classes="guardian-legacy-middle type-body"
                   VerticalAlignment="Center"
                   Text="{Binding Guardian.GuardianStatusDetail}"
                   TextWrapping="Wrap" />
        <TextBlock Grid.Row="3"
                   Classes="guardian-legacy-small guardian-legacy-cyan type-detail"
                   Text="{Binding Guardian.GuardianOriginFooter}"
                   TextWrapping="Wrap" />
      </Grid>

      <Grid RowDefinitions="Auto,Auto,*,Auto"
            IsVisible="{Binding Guardian.IsGuardianOnFootRelicVisible}">
        <TextBlock Classes="overlay-header type-header"
                   Text="{Binding Guardian.GuardianStatusTitle}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
        <TextBlock Grid.Row="2"
                   Classes="guardian-legacy-middle type-body"
                   VerticalAlignment="Center"
                   Text="{Binding Guardian.GuardianStatusDetail}"
                   TextWrapping="Wrap" />
        <TextBlock Grid.Row="3"
                   Classes="guardian-legacy-small guardian-legacy-cyan type-detail"
                   Text="{Binding Guardian.GuardianOnFootFooter}"
                   TextWrapping="Wrap" />
      </Grid>

      <Grid RowDefinitions="Auto,Auto,*,Auto"
            IsVisible="{Binding Guardian.IsGuardianObeliskVisible}">
        <TextBlock Classes="overlay-header type-header"
                   Text="{Binding Guardian.GuardianStatusObeliskTitle}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
        <StackPanel Grid.Row="2"
                    VerticalAlignment="Center"
                    Spacing="2">
          <StackPanel Orientation="Horizontal" Spacing="3">
            <TextBlock Classes="guardian-legacy-middle type-body"
                       VerticalAlignment="Center"
                       Text="Requires:" />
            <ItemsControl ItemsSource="{Binding Guardian.GuardianStatusObeliskArtifacts}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <StackPanel Orientation="Horizontal" Spacing="2" />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="vm:GuardianArtifactRequirementViewModel">
                  <StackPanel Orientation="Horizontal" Spacing="2">
                    <controls:GuardianArtifactGlyphControl
                        Width="16"
                        Height="14"
                        ArtifactCode="{Binding Code}" />
                    <TextBlock Classes="guardian-legacy-small guardian-artifact type-detail"
                               Classes.missing="{Binding !IsMet}"
                               VerticalAlignment="Center"
                               Text="{Binding DisplayName}" />
                    <TextBlock Classes="guardian-legacy-small type-detail"
                               VerticalAlignment="Center"
                               Text="{Binding SeparatorText}" />
                  </StackPanel>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
          <TextBlock Classes="guardian-legacy-small guardian-legacy-cyan type-detail"
                     Text="{Binding Guardian.GuardianStatusObeliskMissionStatus}"
                     TextWrapping="Wrap" />
          <TextBlock Classes="guardian-legacy-small guardian-legacy-warning type-detail"
                     IsVisible="{Binding Guardian.HasGuardianMaterialCapacityWarning}"
                     Text="{Binding Guardian.GuardianMaterialCapacityWarning}"
                     TextWrapping="Wrap" />
        </StackPanel>
        <Grid Grid.Row="3" RowDefinitions="Auto,Auto">
          <TextBlock Classes="guardian-legacy-small guardian-legacy-cyan type-detail"
                     Text="{Binding Guardian.GuardianStatusObeliskFooter}"
                     TextWrapping="Wrap" />
          <TextBlock Grid.Row="1"
                     Classes="guardian-legacy-small type-detail"
                     HorizontalAlignment="Right"
                     Text="{Binding Guardian.GuardianStatusObeliskScanStatus}" />
        </Grid>
      </Grid>

      <Grid RowDefinitions="Auto,Auto,*,Auto"
            IsVisible="{Binding Guardian.IsGuardianPoiChoiceVisible}">
        <TextBlock Classes="overlay-header type-header"
                   Text="{Binding Guardian.GuardianStatusTitle}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
        <Grid Grid.Row="2"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              ColumnDefinitions="Auto,Auto,Auto"
              ColumnSpacing="10">
          <Border Classes="guardian-legacy-choice"
                  Classes.selected="{Binding Guardian.IsGuardianChoiceOneSelected}">
            <TextBlock Classes="guardian-legacy-middle type-body"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding Guardian.GuardianChoiceOneText}"
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
          </Border>
          <Border Grid.Column="1"
                  Classes="guardian-legacy-choice"
                  Classes.selected="{Binding Guardian.IsGuardianChoiceTwoSelected}">
            <TextBlock Classes="guardian-legacy-middle type-body"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding Guardian.GuardianChoiceTwoText}"
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
          </Border>
          <Border Grid.Column="2"
                  Classes="guardian-legacy-choice"
                  Classes.selected="{Binding Guardian.IsGuardianChoiceThreeSelected}"
                  IsVisible="{Binding Guardian.IsGuardianChoiceThreeVisible}">
            <TextBlock Classes="guardian-legacy-middle type-body"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding Guardian.GuardianChoiceThreeText}"
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
          </Border>
        </Grid>
        <TextBlock Grid.Row="3"
                   Classes="guardian-legacy-small guardian-legacy-cyan type-detail"
                   Text="{Binding Guardian.GuardianChoiceGestureText}"
                   TextWrapping="Wrap" />
      </Grid>

      <Grid RowDefinitions="Auto,Auto,*,Auto"
            IsVisible="{Binding Guardian.IsGuardianNoPointVisible}">
        <TextBlock Classes="overlay-header type-header"
                   Text="{Binding Guardian.GuardianStatusTitle}"
                   TextWrapping="Wrap" />
        <Border Grid.Row="1" Classes="guardian-header-rule general-header-rule" />
        <TextBlock Grid.Row="2"
                   Classes="guardian-legacy-middle type-body"
                   VerticalAlignment="Center"
                   Text="{Binding Guardian.GuardianStatusDetail}"
                   TextWrapping="Wrap" />
        <TextBlock Grid.Row="3"
                   Classes="guardian-legacy-small type-detail"
                   Text="{Binding Guardian.SiteDistanceText}"
                   TextWrapping="Wrap" />
      </Grid>
    </Grid>
    </Grid>
  </Border>
</UserControl>
