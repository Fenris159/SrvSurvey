<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:SrvSurvey.Desktop.ViewModels"
             x:Class="SrvSurvey.Desktop.SurfaceMiningSurveyOverlayPresentation"
             x:DataType="vm:MineMapViewModel"
             MinWidth="360"
             MaxWidth="720"
             MaxHeight="126"
             ClipToBounds="True">
  <Border Classes="overlay-panel"
          Padding="10,8"
          Background="{DynamicResource RavenWindowBrush}"
          BorderBrush="{DynamicResource RavenBorderBrush}"
          BorderThickness="1"
          CornerRadius="6">
    <Grid RowDefinitions="Auto,Auto,Auto,Auto" RowSpacing="3">
      <TextBlock Classes="overlay-header"
                 Text="{Binding SurveyGuideTitle}"
                 TextTrimming="CharacterEllipsis" />
      <TextBlock Grid.Row="1"
                 FontSize="12"
                 Text="{Binding SurveyGuideInstruction}"
                 TextTrimming="CharacterEllipsis" />
      <TextBlock Grid.Row="2"
                 Classes="monospace"
                 Foreground="{DynamicResource RavenAccentBrush}"
                 FontSize="11"
                 Text="{Binding SurveyGuideCommandHint}"
                 TextTrimming="CharacterEllipsis" />
      <TextBlock Grid.Row="3"
                 Classes="muted"
                 FontSize="11"
                 IsVisible="{Binding HasSurveyGuideFeedback}"
                 Text="{Binding SurveyGuideFeedback}"
                 TextTrimming="CharacterEllipsis" />
    </Grid>
  </Border>
</UserControl>
