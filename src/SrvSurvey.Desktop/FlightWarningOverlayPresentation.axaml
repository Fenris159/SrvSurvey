<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SrvSurvey.Desktop.FlightWarningOverlayPresentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:SrvSurvey.Desktop.ViewModels"
             x:DataType="vm:SystemSurveyOverlayViewModel"
             mc:Ignorable="d"
             ClipToBounds="False">
  <Border Margin="0"
          Padding="4"
          Background="{Binding Survey.FlightWarningDimBrush}"
          CornerRadius="6">
    <Border Margin="0"
          Padding="4"
          Background="{DynamicResource RavenWindowBrush}"
          BorderThickness="0"
          CornerRadius="5">
      <Grid ColumnDefinitions="Auto,*,Auto"
            RowDefinitions="Auto,Auto"
            ColumnSpacing="6"
            RowSpacing="2">
        <Border MinWidth="24"
                MinHeight="24"
                Padding="4,1"
                Background="{Binding Survey.FlightWarningBrush}"
                CornerRadius="999">
          <Grid>
            <TextBlock FontSize="13" Classes="type-icon" HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Foreground="White"
                     FontWeight="Bold"
                     IsVisible="{Binding !Survey.IsExtremeFlightWarning}"
                     Text="!" />
            <TextBlock FontSize="13" Classes="type-icon" HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="White"
                       IsVisible="{Binding Survey.IsExtremeFlightWarning}"
                       Text="💀" />
          </Grid>
        </Border>
        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center"
                    Spacing="1">
          <TextBlock Classes="overlay-header type-header"
                     Foreground="{Binding Survey.FlightWarningBrush}"
                     Text="FLIGHT WARNING" />
          <TextBlock FontSize="13" Classes="type-title" Foreground="{Binding Survey.FlightWarningBrush}"
                     FontWeight="Bold"
                     Text="{Binding Survey.FlightWarningText}"
                     TextTrimming="CharacterEllipsis" />
        </StackPanel>
        <LayoutTransformControl Grid.Column="2" Classes="type-icon" VerticalAlignment="Center">
          <Border Classes="badge">
            <TextBlock FontSize="8" Foreground="{DynamicResource RavenAccentBrush}"
                       FontWeight="SemiBold"
                       Text="{Binding InputMode}" />
          </Border>
        </LayoutTransformControl>
        <TextBlock FontSize="9" Classes="type-detail" Grid.Row="1"
                   Grid.ColumnSpan="3"
                   HorizontalAlignment="Center"
                   Foreground="#A8B0B8"
                   FontFamily="avares://SrvSurvey.Desktop/Assets/Fonts/Rajdhani#Rajdhani"
                   Text="{Binding Survey.FlightWarningNote}"
                   TextAlignment="Center" />
      </Grid>
    </Border>
  </Border>
</UserControl>
