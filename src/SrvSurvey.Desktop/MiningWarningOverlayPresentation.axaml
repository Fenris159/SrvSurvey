<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="SrvSurvey.Desktop.MiningWarningOverlayPresentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:SrvSurvey.Desktop.ViewModels"
             x:DataType="vm:SurfaceMiningOverlayViewModel"
             mc:Ignorable="d"
             ClipToBounds="True">
  <Border Margin="0"
          Padding="4"
          Background="#4CFF4500"
          CornerRadius="6">
    <Border Margin="0"
          Padding="4"
          Background="{DynamicResource RavenWindowBrush}"
          BorderThickness="0"
          CornerRadius="5">
      <Grid ColumnDefinitions="Auto,*,Auto"
            RowDefinitions="Auto,Auto"
            ColumnSpacing="6"
            RowSpacing="2">
        <Border Width="24"
                Height="24"
                Background="#FF4500"
                CornerRadius="12">
          <Grid>
            <TextBlock HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Foreground="White"
                     FontSize="13"
                     FontWeight="Bold"
                     Text="!" />
          </Grid>
        </Border>
        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center"
                    Spacing="1">
          <TextBlock Classes="overlay-header"
                     Foreground="#FF4500"
                     Text="WARNING" />
          <TextBlock Foreground="#FF4500"
                     FontSize="13"
                     FontWeight="Bold"
                     Text="TOO FAR FROM RIGS"
                     TextTrimming="CharacterEllipsis" />
        </StackPanel>
        <Border Grid.Column="2"
                Classes="badge"
                VerticalAlignment="Center">
          <TextBlock Foreground="{DynamicResource RavenAccentBrush}"
                     FontSize="8"
                     FontWeight="SemiBold"
                     Text="{Binding InputMode}" />
        </Border>
        <TextBlock Grid.Row="1"
                   Grid.ColumnSpan="3"
                   HorizontalAlignment="Center"
                   Foreground="#A8B0B8"
                   FontFamily="avares://SrvSurvey.Desktop/Assets/Fonts/Rajdhani#Rajdhani"
                   FontSize="9"
                   Text="Moving beyond 4.5Km will Destroy Rigs"
                   TextAlignment="Center" />
      </Grid>
    </Border>
  </Border>
</UserControl>
