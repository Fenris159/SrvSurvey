<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:SrvSurvey.Desktop.ViewModels"
             x:Class="SrvSurvey.Desktop.Views.SettingsView"
             x:DataType="vm:MainWindowViewModel"
             mc:Ignorable="d">
  <ScrollViewer HorizontalScrollBarVisibility="Disabled">
    <StackPanel Margin="32" Spacing="24" MaxWidth="1100">
      <StackPanel Spacing="5">
        <TextBlock Classes="eyebrow" Text="APPEARANCE" />
        <TextBlock Classes="page-title" Text="Themes" />
        <TextBlock Classes="muted"
                   Text="Use the same five visual identities available on Raven Colonial." />
      </StackPanel>

      <Border Classes="card raised">
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="20">
          <StackPanel Spacing="4">
            <TextBlock Classes="card-title" Text="Current theme" />
            <TextBlock Classes="muted" Text="{Binding SelectedThemeName}" />
          </StackPanel>
          <Border Grid.Column="1" Classes="badge" VerticalAlignment="Center">
            <TextBlock Foreground="{DynamicResource RavenAccentBrush}"
                       FontWeight="SemiBold"
                       Text="SAVED LOCALLY" />
          </Border>
        </Grid>
      </Border>

      <ItemsControl ItemsSource="{Binding ThemeOptions}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel Orientation="Horizontal" ItemWidth="250" ItemHeight="184" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate x:DataType="vm:ThemeOptionViewModel">
            <Button Classes="secondary"
                    Width="238"
                    Height="172"
                    Margin="0,0,12,12"
                    Padding="0"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Command="{Binding SelectCommand}">
              <Border Background="{Binding WindowBrush}"
                      BorderBrush="{Binding AccentBrush}"
                      BorderThickness="1"
                      CornerRadius="8"
                      Padding="16">
                <Grid RowDefinitions="*,Auto" RowSpacing="14">
                  <StackPanel Spacing="8">
                    <Grid ColumnDefinitions="*,Auto">
                      <TextBlock Foreground="{Binding TextBrush}"
                                 FontSize="15"
                                 FontWeight="SemiBold"
                                 Text="{Binding DisplayName}" />
                      <TextBlock Grid.Column="1"
                                 Foreground="{Binding AccentBrush}"
                                 FontSize="10"
                                 FontWeight="Bold"
                                 Text="{Binding ModeLabel}" />
                    </Grid>
                    <Border Height="48"
                            Background="{Binding SurfaceBrush}"
                            CornerRadius="6"
                            Padding="10">
                      <Grid ColumnDefinitions="*,Auto" VerticalAlignment="Center">
                        <Border Width="72" Height="7"
                                HorizontalAlignment="Left"
                                Background="{Binding TextBrush}"
                                CornerRadius="4" />
                        <Border Grid.Column="1" Width="38" Height="20"
                                Background="{Binding AccentBrush}"
                                CornerRadius="5" />
                      </Grid>
                    </Border>
                  </StackPanel>
                  <Border Grid.Row="1" Height="4"
                          Background="{Binding AccentBrush}"
                          CornerRadius="2" />
                </Grid>
              </Border>
            </Button>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

      <Border Classes="card">
        <StackPanel Spacing="8">
          <TextBlock Classes="card-title" Text="Theme compatibility" />
          <TextBlock Classes="muted"
                     TextWrapping="Wrap"
                     Text="These choices style the Avalonia shell and use native light/dark control behavior. Importing legacy custom orange/cyan theme.json values for game overlays remains part of the overlay port." />
          <TextBlock Foreground="{DynamicResource RavenWarningBrush}"
                     TextWrapping="Wrap"
                     Text="{Binding ThemeStatusMessage}" />
        </StackPanel>
      </Border>

      <Separator />

      <StackPanel Spacing="5">
        <TextBlock Classes="eyebrow" Text="OVERLAYS" />
        <TextBlock Classes="page-title" Text="Next-jump intelligence" />
        <TextBlock Classes="muted"
                   TextWrapping="Wrap"
                   Text="Control the passive overlay shown while charging or travelling through hyperspace." />
      </StackPanel>

      <Border Classes="card raised">
        <StackPanel Spacing="14">
          <CheckBox IsChecked="{Binding JumpInfo.AutoShow, Mode=TwoWay}"
                    Content="Show next-jump information automatically" />
          <TextBlock Margin="28,-8,0,0"
                     Classes="muted"
                     TextWrapping="Wrap"
                     Text="Displays target, route progress, discovery, traffic, services, Guardian sites, and followed-route notes while the FSD is charging or jumping." />
          <CheckBox IsChecked="{Binding JumpInfo.Minimal, Mode=TwoWay}"
                    Content="Use the compact overlay" />
          <TextBlock Margin="28,-8,0,0"
                     Classes="muted"
                     TextWrapping="Wrap"
                     Text="Keeps the target and route visualization while hiding network metadata and special locations." />
          <CheckBox IsChecked="{Binding JumpInfo.ShowWhenNextHopSelected, Mode=TwoWay}"
                    Content="Show when the followed-route next hop is selected" />
          <TextBlock Margin="28,-8,0,0"
                     Classes="muted"
                     TextWrapping="Wrap"
                     Text="Also displays the overlay in supercruise when the navigation destination matches the next followed-route system." />
          <TextBlock Foreground="{DynamicResource RavenWarningBrush}"
                     IsVisible="{Binding JumpInfo.HasSettingsStatus}"
                     TextWrapping="Wrap"
                     Text="{Binding JumpInfo.SettingsStatus}" />
        </StackPanel>
      </Border>

      <Border Classes="card raised">
        <StackPanel Spacing="16">
          <StackPanel Spacing="4">
            <TextBlock Classes="card-title" Text="System survey overlays" />
            <TextBlock Classes="muted"
                       TextWrapping="Wrap"
                       Text="Configure the FSS body feed and compact system-completion overlay. These preserve the original survey thresholds while using the active Raven theme." />
          </StackPanel>

          <Grid ColumnDefinitions="*,*" ColumnSpacing="28">
            <StackPanel Spacing="12">
              <TextBlock Classes="eyebrow" Text="FSS BODY FEED" />
              <CheckBox IsChecked="{Binding SystemSurvey.AutoShowFssInfo, Mode=TwoWay}"
                        Content="Show automatically in FSS" />
              <CheckBox IsChecked="{Binding SystemSurvey.ShowFssInfoInSystemMap, Mode=TwoWay}"
                        Content="Also show in the system map" />
              <CheckBox IsChecked="{Binding SystemSurvey.ShowFssInfoInNavigationPanel, Mode=TwoWay}"
                        Content="Also show in the navigation panel" />
              <CheckBox IsChecked="{Binding SystemSurvey.HideGeoCount, Mode=TwoWay}"
                        Content="Hide geological signal counts" />
              <Grid ColumnDefinitions="*,150" ColumnSpacing="12">
                <TextBlock VerticalAlignment="Center"
                           Text="Minimum displayed body value" />
                <NumericUpDown Grid.Column="1"
                               Minimum="0"
                               Maximum="100000000"
                               Increment="1000"
                               FormatString="N0"
                               Value="{Binding SystemSurvey.FssBodyValueFloor, Mode=TwoWay}" />
              </Grid>
            </StackPanel>

            <StackPanel Grid.Column="1" Spacing="12">
              <TextBlock Classes="eyebrow" Text="SURVEY STATUS" />
              <CheckBox IsChecked="{Binding SystemSurvey.AutoShowSystemStatus, Mode=TwoWay}"
                        Content="Show system completion status" />
              <CheckBox IsChecked="{Binding SystemSurvey.HighlightDssCandidates, Mode=TwoWay}"
                        Content="Skip and highlight by DSS value" />
              <CheckBox IsChecked="{Binding SystemSurvey.SkipGasGiantsForDss, Mode=TwoWay}"
                        Content="Skip gas giants for DSS" />
              <CheckBox IsChecked="{Binding SystemSurvey.SkipRingsForDss, Mode=TwoWay}"
                        Content="Skip rings for DSS" />
              <CheckBox IsChecked="{Binding SystemSurvey.SkipDistantDssCandidates, Mode=TwoWay}"
                        Content="Skip distant DSS candidates" />
              <CheckBox IsChecked="{Binding SystemSurvey.ShowNonBodySignals, Mode=TwoWay}"
                        Content="Show non-body signal count" />
              <Grid ColumnDefinitions="*,150" ColumnSpacing="12">
                <TextBlock VerticalAlignment="Center"
                           Text="Minimum DSS value" />
                <NumericUpDown Grid.Column="1"
                               Minimum="0"
                               Maximum="100000000"
                               Increment="10000"
                               FormatString="N0"
                               Value="{Binding SystemSurvey.DssValueFloor, Mode=TwoWay}" />
              </Grid>
              <Grid ColumnDefinitions="*,150" ColumnSpacing="12">
                <TextBlock VerticalAlignment="Center"
                           Text="DSS distance limit (LS)" />
                <NumericUpDown Grid.Column="1"
                               Minimum="0"
                               Maximum="10000000"
                               Increment="1000"
                               FormatString="N0"
                               Value="{Binding SystemSurvey.DssDistanceLimitLs, Mode=TwoWay}" />
              </Grid>
            </StackPanel>
          </Grid>

          <TextBlock Foreground="{DynamicResource RavenWarningBrush}"
                     IsVisible="{Binding SystemSurvey.HasSettingsStatus}"
                     TextWrapping="Wrap"
                     Text="{Binding SystemSurvey.SettingsStatus}" />
        </StackPanel>
      </Border>

      <Separator />

      <StackPanel Spacing="5">
        <TextBlock Classes="eyebrow" Text="INPUT" />
        <TextBlock Classes="page-title" Text="Global shortcuts" />
        <TextBlock Classes="muted"
                   TextWrapping="Wrap"
                   Text="Use the original SrvSurvey shortcuts while either this application or Elite Dangerous has focus." />
      </StackPanel>

      <Border Classes="card raised">
        <StackPanel Spacing="14">
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="20">
            <StackPanel Spacing="4">
              <TextBlock Classes="card-title" Text="Keyboard hook" />
              <TextBlock Classes="muted"
                         TextWrapping="Wrap"
                         Text="Keyboard capture stays off until you enable it here. Synthetic key events are ignored." />
            </StackPanel>
            <CheckBox Grid.Column="1"
                      VerticalAlignment="Center"
                      IsChecked="{Binding InputSettings.KeyboardEnabled, Mode=TwoWay}"
                      IsEnabled="{Binding InputSettings.IsKeyboardAvailable}"
                      Content="Enabled" />
          </Grid>
          <TextBlock Foreground="{DynamicResource RavenAccentBrush}"
                     TextWrapping="Wrap"
                     Text="{Binding InputSettings.RuntimeStatus}" />
          <TextBlock Foreground="{DynamicResource RavenWarningBrush}"
                     IsVisible="{Binding InputSettings.HasPersistenceStatus}"
                     TextWrapping="Wrap"
                     Text="{Binding InputSettings.PersistenceStatus}" />
          <TextBlock Classes="muted"
                     IsVisible="{Binding InputSettings.HasLastActionStatus}"
                     TextWrapping="Wrap"
                     Text="{Binding InputSettings.LastActionStatus}" />
        </StackPanel>
      </Border>

      <Expander HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                IsExpanded="False">
        <Expander.Header>
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="20">
            <StackPanel Spacing="3">
              <TextBlock Classes="card-title" Text="Shortcut bindings" />
              <TextBlock Classes="muted"
                         Text="Expand to edit all original keyboard actions." />
            </StackPanel>
            <Button Grid.Column="1"
                    Classes="secondary"
                    Command="{Binding InputSettings.ResetBindingsCommand}"
                    Content="Restore defaults" />
          </Grid>
        </Expander.Header>
        <ItemsControl Margin="0,14,0,0"
                      ItemsSource="{Binding InputSettings.Bindings}">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="vm:InputBindingViewModel">
              <Border BorderBrush="{DynamicResource RavenBorderBrush}"
                      BorderThickness="0,1,0,0"
                      Padding="4,12">
                <Grid ColumnDefinitions="*,220" ColumnSpacing="20">
                  <StackPanel Spacing="3">
                    <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" />
                    <TextBlock Classes="muted"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Text="{Binding Description}" />
                  </StackPanel>
                  <StackPanel Grid.Column="1" Spacing="3">
                    <TextBox Text="{Binding Chord, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                             PlaceholderText="Leave blank to disable" />
                    <TextBlock Classes="muted"
                               FontSize="10"
                               Text="{Binding DefaultChord}" />
                    <TextBlock Foreground="{DynamicResource RavenWarningBrush}"
                               FontSize="10"
                               IsVisible="{Binding HasValidationError}"
                               TextWrapping="Wrap"
                               Text="{Binding ValidationMessage}" />
                  </StackPanel>
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </Expander>

      <Border Classes="card raised">
        <StackPanel Spacing="14">
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="20">
            <StackPanel Spacing="4">
              <TextBlock Classes="card-title" Text="Controller input" />
              <TextBlock Classes="muted"
                         TextWrapping="Wrap"
                         Text="SDL supports standard gamepads, joysticks, and HOTAS devices. Input is accepted while SrvSurvey or Elite Dangerous has focus." />
            </StackPanel>
            <CheckBox Grid.Column="1"
                      VerticalAlignment="Center"
                      IsChecked="{Binding InputSettings.ControllerEnabled, Mode=TwoWay}"
                      IsEnabled="{Binding InputSettings.CanEnableControllerInput}"
                      Content="Enabled" />
          </Grid>

          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
            <ComboBox MinWidth="360"
                      ItemsSource="{Binding InputSettings.ControllerDevices}"
                      SelectedItem="{Binding InputSettings.SelectedController, Mode=TwoWay}"
                      PlaceholderText="Select a connected controller">
              <ComboBox.ItemTemplate>
                <DataTemplate x:DataType="vm:ControllerDeviceOptionViewModel">
                  <StackPanel Margin="4" Spacing="2">
                    <TextBlock FontWeight="SemiBold"
                               Text="{Binding DisplayName}" />
                    <TextBlock Classes="muted"
                               FontSize="11"
                               Text="{Binding Description}"
                               TextTrimming="CharacterEllipsis" />
                  </StackPanel>
                </DataTemplate>
              </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Grid.Column="1"
                    Classes="secondary"
                    Command="{Binding InputSettings.RefreshControllersCommand}"
                    Content="Refresh devices" />
          </Grid>

          <TextBlock Classes="muted"
                     TextWrapping="Wrap"
                     Text="{Binding InputSettings.ControllerDiscoveryStatus}" />
          <TextBlock Foreground="{DynamicResource RavenAccentBrush}"
                     TextWrapping="Wrap"
                     Text="{Binding InputSettings.ControllerRuntimeStatus}" />
        </StackPanel>
      </Border>

      <Separator />

      <StackPanel Spacing="5">
        <TextBlock Classes="eyebrow" Text="DATA &amp; MIGRATION" />
        <TextBlock Classes="page-title" Text="Legacy profile" />
        <TextBlock Classes="muted"
                   TextWrapping="Wrap"
                   Text="Bring settings, commander state, surveys, routes, journeys, caches, and every other legacy profile file into the cross-platform application." />
      </StackPanel>

      <Border Classes="card raised">
        <StackPanel Spacing="16">
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="20">
            <StackPanel Spacing="5">
              <TextBlock Classes="card-title" Text="Detected source" />
              <ComboBox MinWidth="360"
                        ItemsSource="{Binding LegacyProfiles}"
                        SelectedItem="{Binding SelectedLegacyProfile}">
                <ComboBox.ItemTemplate>
                  <DataTemplate x:DataType="vm:LegacyProfileOptionViewModel">
                    <StackPanel Margin="4" Spacing="2">
                      <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" />
                      <TextBlock Classes="muted"
                                 FontSize="11"
                                 Text="{Binding Path}"
                                 TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                  </DataTemplate>
                </ComboBox.ItemTemplate>
              </ComboBox>
            </StackPanel>
            <Button Grid.Column="1"
                    Classes="primary"
                    VerticalAlignment="Bottom"
                    Command="{Binding ImportLegacyProfileCommand}"
                    Content="{Binding ImportProfileButtonText}" />
          </Grid>

          <Grid ColumnDefinitions="Auto,*"
                RowDefinitions="Auto,Auto"
                ColumnSpacing="12"
                RowSpacing="6">
            <TextBlock Classes="muted" Text="New profile" />
            <TextBlock Grid.Column="1"
                       TextWrapping="Wrap"
                       Text="{Binding ProfileDataDirectory}" />
            <TextBlock Grid.Row="1" Classes="muted" Text="Backups" />
            <TextBlock Grid.Row="1" Grid.Column="1"
                       TextWrapping="Wrap"
                       Text="{Binding ProfileBackupDirectory}" />
          </Grid>

          <TextBlock Foreground="{DynamicResource RavenWarningBrush}"
                     TextWrapping="Wrap"
                     Text="{Binding ProfileStatusMessage}" />
        </StackPanel>
      </Border>
    </StackPanel>
  </ScrollViewer>
</UserControl>
